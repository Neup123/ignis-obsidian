services:
  ignis:
    build:
      context: ../..
      dockerfile: apps/ignis-server/Dockerfile
    ports:
      - "8082:8080"
    volumes:
      - ./vaults:/vaults
      - ./data:/app/data
      - obsidian-app:/app/obsidian-app
    restart: unless-stopped

volumes:
  obsidian-app:
